set-option -s repeat-time 0
set-option -sg escape-time 0

set -g mode-keys vi

# Leave the terminal window title alone (oh-my-tmux turns this on)
set -g set-titles off

# TPM
set -g @plugin 'tmux-plugins/tpm'

# Common bindings
set -g @plugin 'tmux-plugins/tmux-sensible'

# Destroy window without detaching
set-option -g detach-on-destroy off

# Themes pack
# NOTE: disabled — themepack applies its default theme when TPM runs, which
# overrides any theme sourced from this file.
# set -g @plugin 'jimeh/tmux-themepack'

# Themes

# Nord
# set -g @plugin 'arcticicestudio/nord-tmux'

# Cyan
# set -g @themepack 'powerline/block/cyan'

# set -g @themepack 'powerline/block/gray'

# Rose Pine
# set -g @plugin 'rose-pine/tmux'
# set -g @rose_pine_variant 'main'

# Catppuccin
# set -g @plugin 'catppuccin/tmux#latest'
# set -g @catppuccin_flavor 'mocha' # latte,frappe, macchiato or mocha
# set -g @catppuccin_status_modules_right ''

# Flexoki Dark
# source-file ./flexoki-dark.tmuxtheme

# Ayu Dark
# oh-my-tmux applies its own theme after this file is sourced, so turn it off
# and let ayu-dark.tmuxtheme (sourced at the bottom) own the status bar
tmux_conf_theme="disabled"

# End of themes

# Windows session
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @continuum-restore 'on'

run '~/.tmux/plugins/tpm/tpm'

# Theme last, so it wins over anything a plugin sets.
# NOTE: a relative path here resolves against the CURRENT WORKING DIRECTORY of
# whoever starts tmux, not against this file — so it silently fails when the
# server is started at boot (by tmux-continuum) from / or $HOME. Resolve the
# repo through the ~/.tmux.conf.local symlink instead.
run 'tmux source-file "$(dirname "$(readlink -f "$TMUX_CONF_LOCAL")")/ayu-dark.tmuxtheme"'

